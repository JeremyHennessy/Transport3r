# UI test intent

The protected branch validation should be used as the first executable test environment because the current container cannot resolve github.com for a local clone. No merge or production claim should be made until CI and hosted verification succeed.
