# Continued engineering custody - September 9, 2026

## Verified starting baseline

Starting local main, remote main and hosted SHA: `f19708ee2b0fe42a3b0a39c21459aa244aa2b955`. This advanced the supplied `03ce21f7017b1ec2a7583aa7fec2d9f735481213` checkpoint through PR #38 (SMS directory/Summary display, passenger percentile parsing and mobile layout). Pages run `34360864857` and CI run `34360209500` succeeded. Hosted verification reconfirmed six asset hashes, 36 sources and 810 fields. These are technical verification checkpoints, not user visual approval.

Open work: only research PR #17, head `7d61b32268e089fb0da090628641ef2b5f6bad0a`. No open issues returned. Research remains frozen and separate.

Branch protection was configured and read back successfully: PR required; GitHub Actions `validate` required with up-to-date branch; administrators included; no force pushes or deletion; conversation resolution required. Zero additional approvals avoids a single-owner deadlock. Pages remains a post-merge workflow, not a required pre-merge check.

Existing SQLite preserves three cuts (baseline, 14-source v1, 18-source v2), 392,092 total versioned records across 62 carriers. Latest v2 cut: `20260909T121214040360Z-20d16b62`. This is not complete 36-source coverage or historical training data.

## Increment 1 - explanations, maps and reports

New files: `insightModel.ts`, `CarrierInsight.tsx`, `eventGeography.ts`, `SafetyVisuals.tsx`, `stateBoundaries.json`, `CarrierReport.tsx`, `reportModel.ts`, scoped insight/report styles and dedicated tests. Existing route files receive narrow additions. No score formulas or unrelated layout changes.

- At-a-glance narratives cover all major Carrier 360 sections; bounded daily charts, violation/make distributions, authority/filing event timelines and a 36-source coverage matrix explain existing tables.
- Safety maps synchronize with the applied daily event window. Clusters use inspection `county_code_state`, falling back to explicitly labelled `report_state` jurisdiction; crashes use `state`. Neither carrier address nor vehicle-license state supplies event geography. Unknown states remain unmapped. Symbols link verified inspection IDs; layers toggle separately.
- Official FMCSA dictionaries: Inspection Rev05 2025-10-15 (asset `75be4272-c17d-4b6f-9613-bf37c9efab22`); Crash Rev05 2025-11-04 (asset `9855bbf8-aef9-4fd8-80e5-137cf21db446`). No coordinate fields are registered. State/county text supports only a labelled regional display here; city descriptions are not geocoded.
- Boundary source: [Census 2025 state boundaries, 20M](https://www2.census.gov/geo/tiger/GENZ2025/shp/cb_2025_us_state_20m.zip). Bundled US-government geography; no geocoding, tiles, credentials, carrier-location transfers or runtime boundary requests. State symbols are display locations, not event coordinates. AK/HI/PR use insets.
- `#/carrier/{dot}/report?format=brief|detailed&start=YYYY-MM-DD&end=YYYY-MM-DD` provides print-to-PDF and a complete returned-field JSON archive. The brief has three designed A4 pages; the appendix retains all loaded rows with readable key fields and explicit bounds. Static Pages architecture is preserved. No runtime PDF dependency was added; pypdf is verification-only.
- Local acceptance: 125 JS + 64 Python tests, all existing browser scenarios, 11 new insight/map interactions, 16 PDF fixture exports covering eight requested carrier types. Every brief has three pages; fixture appendices have 10-12 pages. Live-source reports for DOTs 3938496/3706/2855794 each have a three-page brief; their appendices have 12/252/11 pages. Large appendices can be lengthy because returned rows are retained.
- Local evidence: `outputs/insights-reports-local`, `outputs/report-validation`, `outputs/insight-browser.json`. Hosted verification and resulting SHA are recorded after release, separately from local results.

## Initial ordered work (superseded by the acceptance log below)

1. Release and verify Increment 1 independently.
2. Extend complete acquisition/verification beyond v2 to the remaining MOTUS and defensible legacy docket sources; preserve every earlier cut.
3. Establish exact SMS month and source/exposure eligibility. Acquisition timestamps alone cannot satisfy this requirement.
4. Separate insurer portfolio storage contract, durable prior/current alert comparisons and knowledge-time panel gates.
5. Mature future labels, TRI v0.2 and forecasting remain conditional on historical/outcome eligibility; never fill missing history with today's observations.
6. Matched-cut Power BI execution and enrichment evaluation require their own evidence; no formula copying or name-only joins.

Each subsequent increment must record tested head, PR, merge SHA, CI, Pages, hosted artifacts, rendered behavior and remaining limitations.

## Increment 1 production acceptance

PR [#39](https://github.com/JeremyHennessy/Transport3r/pull/39), tested head `f02fd756beb888479dca653a8a9742923c827d82`, CI `34364298670` passed. Exact-head merge: `257ecffc69c3c290e92ad0918911ad8dc5f81d78`. Pages `34365386369` passed. Hosted verifier passed all six asset hashes, 36 sources and 810 fields. All seven major insight sections, synchronized state map desktop/mobile and both report routes rendered without runtime errors. Public-app PDF examples: DOT 3938496 brief 3 / detailed 12 pages; 3706 brief 3 / detailed 252 pages; 2855794 brief 3 / detailed 11 pages. Evidence: `outputs/insight-hosted-release.json`, `outputs/insights-reports-live/gallery.html`, `results.json`, `pdf-page-counts.json` and adjacent PDFs/screenshots. Large appendices retain returned evidence, so their length is intentional.

## Increment 2 - warehouse, legacy identity and private foundations

Starting deployed main: `257ecffc69c3c290e92ad0918911ad8dc5f81d78`. Layers changed: complete local acquisition/SQLite validation, private portfolio contract, durable public-evidence comparisons, temporal readiness, narrow live legacy queries and source catalog documentation. No TRI formulas, insurer facts, scheduled notifications or hosted private service are introduced.

The initial v3 cut `20260909T143742317695Z-f58a170c` reconciled its queried rows but failed an additional identity-format review: legacy USDOT fields use eight-digit zero padding. It is preserved unchanged for audit, not eligible evidence. Corrected v3 requires identity contract `LEGACY_USDOT_CANONICAL_AND_8_DIGIT_1`; both browser and warehouse query canonical and padded USDOTs, retaining raw IDs and rejecting foreign records. Docket prefixes and docket leading zeros remain exact, and global bridge ownership conflicts block acquisition. Query-count equality alone cannot prove the query found every carrier representation.

The corrected acquisition covers all 36 registered sources, including remaining MOTUS history/differences and legacy docket sources. Previous baseline/v1/v2 cuts remain untouched. Complete means every selected cohort query, not nationwide coverage or common snapshot dates. CI now acquires and validates v3, including child rows and docket identity. A missing v3 identity contract is excluded from warehouse selection and monitoring and is reported as audit-only.

Portfolio is a validated versioned private SQLite import/history foundation, with no invented policies. Alerts persist prior/current values, source observation timestamps and rules; repeated comparisons are idempotent. SMS movement is disabled until exact releases are bound. See [storage and monitoring contract](private-monitoring-foundation.md).

## Explicit analytical gates

The September 9 capture of the [official SMS website](https://ai.fmcsa.dot.gov/SMS/) says snapshot July 31, 2026 and website update August 10, 2026. The saved eight DataHub schemas have no explicit snapshot field. This website date does not certify the release identity of those extracts. HTML and metadata hashes are preserved in `outputs/sms-release-20260909`. The tentative release schedule is not publication evidence.

Current acquisition dates, event dates, Census report/mileage dates and source publication dates remain separate. Exposure eligibility is audited per carrier; future outcomes remain null. A September observation cannot manufacture an August month-end row or mature 90/180/365-day follow-up. No numeric TRI v0.2, forecast or actuarial model is released. PR #17 stays frozen at `7d61b32268e089fb0da090628641ef2b5f6bad0a`.

Power BI's reviewed definitions are useful evidence, but immutable matching input cuts and executed DAX exports are unavailable. Matched-cut numerical reconciliation remains unexecuted. [Source assessment](data-enrichment-review.md) integrates Census geography and distinguishes PHMSA access/identity, SmartWay division identity, NHTSA campaign taxonomy and BTS layer metadata gates. No name-only joins or speculative source facts are introduced.

## Increment 2 data acceptance

Corrected cut: `20260909T145454934865Z-383f1a14`, acquired September 9, 2026. Verified 62 carriers / 36 sources / 213,453 rows, including 102,984 inspection child rows and 977 legacy records. The global exact-docket ownership proof retained 30 rows for 30 dockets; 43 docket-only insurance rows mapped successfully. All 2,232 carrier/source counts reconcile. Promotion and warehouse integrity passed, preserving four eligible cuts / 605,545 versioned records, plus the separate 212,476-row audit-only cut. Evidence: `outputs/warehouse-v3-corrected-validation/validation.json`, `outputs/foundation-acceptance/acceptance.json` and adjacent hashed artifacts.

Actual prior-v2/current-v3 comparison persisted four alerts: one newly observed OOS inspection and three violation-record-set changes, zero comparison issues. Repeat returned `ALREADY_COMPARED`. No SMS movement or scheduled delivery is claimed. Private portfolio initialized with zero records. Current as-of panel has 62 rows, zero training-eligible rows and null future labels; unavailable August month end is rejected. Exposure audit flags 17 stale report dates, 23 ineligible mileage years and two missing/nonpositive mileages (overlapping carrier groups).

Local validation: 79 Python tests, 126 JS tests, typecheck/build, all-source mapping for active/inactive examples, date/coverage/inspection/insight browser gates and all 16 PDF fixture files. Every brief remains three pages; PDF text bounds and blank-page checks pass. Live-source local browser verification confirms USDOT 3706 has one legacy carrier row, 116 authority-history rows, 70 insurance-history rows and three docket-insurance rows, with desktop/mobile screenshots and no runtime errors in `outputs/legacy-local`. Resulting release SHA and hosted evidence are recorded separately after deployment.
