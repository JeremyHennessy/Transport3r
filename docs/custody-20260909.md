# Continued engineering custody - September 9, 2026

## Verified starting baseline

Starting local main, remote main and hosted SHA: `f19708ee2b0fe42a3b0a39c21459aa244aa2b955`. This advanced the supplied `03ce21f7017b1ec2a7583aa7fec2d9f735481213` checkpoint through PR #38 (SMS directory/Summary display, passenger percentile parsing and mobile layout). Pages run `34360864857` and CI run `34360209500` succeeded. Hosted verification reconfirmed six asset hashes, 36 sources and 810 fields. These are technical verification checkpoints, not user visual approval.

Open work: only research PR #17, head `7d61b32268e089fb0da090628641ef2b5f6bad0a`. No open issues returned. Research remains frozen and separate.

Branch protection was configured and read back successfully: PR required; GitHub Actions `validate` required with up-to-date branch; administrators included; no force pushes or deletion; conversation resolution required. Zero additional approvals avoids a single-owner deadlock. Pages remains a post-merge workflow, not a required pre-merge check.

Existing SQLite preserves three cuts (baseline, 14-source v1, 18-source v2), 392,092 total versioned records across 62 carriers. Latest v2 cut: `20260909T121214040360Z-20d16b62`. This is not complete 36-source coverage or historical training data.

## Increment 1 - explanations, maps and reports

New files: `insightModel.ts`, `CarrierInsight.tsx`, `eventGeography.ts`, `SafetyVisuals.tsx`, `stateBoundaries.json`, `CarrierReport.tsx`, `reportModel.ts`, scoped insight/report styles and dedicated tests. Existing route files receive narrow additions. No score formulas or unrelated layout changes.

- At-a-glance narratives cover all major Carrier 360 sections; bounded daily charts, violation/make distributions, authority/filing event timelines and a 36-source coverage matrix explain existing tables.
- Safety maps synchronize with the applied daily event window. Clusters use inspection `county_code_state`, falling back to explicitly labelled `report_state` jurisdiction; crashes use `state`. Neither carrier address nor vehicle-license state supplies event geography. Unknown states remain unmapped. Symbols link verified inspection IDs; layers toggle separately.
- Official FMCSA dictionaries: Inspection Rev05 2025-10-15 (asset `75be4272-c17d-4b6f-9613-bf37c9efab22`); Crash Rev05 2025-11-04 (asset `9855bbf8-aef9-4fd8-80e5-137cf21db446`). No coordinate fields are registered. State/county text supports only a labelled regional display here; city descriptions are not geocoded.
- Boundary source: [Census 2025 state boundaries, 20M](https://www2.census.gov/geo/tiger/GENZ2025/shp/cb_2025_us_state_20m.zip). Bundled US-government geography; no geocoding, tiles, credentials, carrier-location transfers or runtime boundary requests. State symbols are display locations, not event coordinates. AK/HI/PR use insets.
- `#/carrier/{dot}/report?format=brief|detailed&start=YYYY-MM-DD&end=YYYY-MM-DD` provides print-to-PDF and a complete returned-field JSON archive. The brief has three designed A4 pages; the appendix retains all loaded rows with readable key fields and explicit bounds. Static Pages architecture is preserved. No runtime PDF dependency was added; pypdf is verification-only.
- Local acceptance: 125 JS + 64 Python tests, all existing browser scenarios, 11 new insight/map interactions, 16 PDF fixture exports covering eight requested carrier types. Every brief has three pages; fixture appendices have 10-12 pages. Live-source reports for DOTs 3938496/3706/2855794 each have a three-page brief; their appendices have 12/252/11 pages. Large appendices can be lengthy because returned rows are retained.
- Local evidence: `outputs/insights-reports-local`, `outputs/report-validation`, `outputs/insight-browser.json`. Hosted verification and resulting SHA are recorded after release, separately from local results.

## Remaining ordered work

1. Release and verify Increment 1 independently.
2. Extend complete acquisition/verification beyond v2 to the remaining MOTUS and defensible legacy docket sources; preserve every earlier cut.
3. Establish exact SMS month and source/exposure eligibility. Acquisition timestamps alone cannot satisfy this requirement.
4. Separate insurer portfolio storage contract, durable prior/current alert comparisons and knowledge-time panel gates.
5. Mature future labels, TRI v0.2 and forecasting remain conditional on historical/outcome eligibility; never fill missing history with today's observations.
6. Matched-cut Power BI execution and enrichment evaluation require their own evidence; no formula copying or name-only joins.

Each subsequent increment must record tested head, PR, merge SHA, CI, Pages, hosted artifacts, rendered behavior and remaining limitations.
